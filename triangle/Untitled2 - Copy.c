#include <stdio.h>

int main ()
{
    int a,b,c ;
    printf ("Please enter length of each side of your triangle: ") ;
    scanf ("%d%d%d",&a, &b, &c) ;

    switch ("%d%d%d",&a, &b, &c)
    {
        case 'a=0&&b=0&&c=0' : printf ("This is NOT a triangle."); break;
        default : printf ("This is NOT a triangle.") ;
    }
    return 0 ;
}
